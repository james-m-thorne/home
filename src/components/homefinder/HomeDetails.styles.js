import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(() => ({
  header: {
    paddingBottom: 0
  },
  closeButton: {
    marginLeft: 'auto'
  },
  smallText: {
    fontSize: 11
  }
}))
