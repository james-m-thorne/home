import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(() => ({
  card: {
    margin: '20px 0',
  },
  header: {
    paddingBottom: 0
  },
  closeButton: {
    marginLeft: 'auto'
  }
}))
